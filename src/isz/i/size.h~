#ifndef ISZ_INTERFACE_SIZE_H
#define ISZ_INTERFACE_SIZE_H

#include <stddef.h>
#include "thing/thing.h"

ISZ_INTERFACE_ID_DECLARE(isz_size);

typedef struct isz_size_i_t_
{
	size_t (*get)(void *vit);
} isz_size_i_t;

#endif /* !ISZ_INTERFACE_SIZE_H */
